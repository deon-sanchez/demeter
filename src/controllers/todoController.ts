import { Request, Response } from "express";

export const getTodo = (_req: Request, res: Response) => {
  res.status(200).json();
};

export const listTodos = (_req: Request, res: Response) => {
  res.status(200).json();
};

export const createTodo = (_req: Request, res: Response) => {
  res.status(200).json();
};

export const removeTodo = (_req: Request, res: Response) => {
  res.status(200).json();
};

export const updateTodo = (_req: Request, res: Response) => {
  res.status(200).json();
};

export const deleteTodo = (_req: Request, res: Response) => {
  res.status(200).json();
};
